[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "csim"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "pygame>=2.0",
    "numpy>=1.20",
]

[project.scripts]
csim = "csim.main:main"
